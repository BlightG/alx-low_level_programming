#include "main.h"
/**
*
*/
int check_n(int n, int index);
{
	if
}

/**
* get_bit - gets bit at index index from n
*
* @n: index comparison
* @index: index to be compared
*
* Return: returns 1 if bit value at index or -1 if faliure
*/
int get_bit(unsigned long int n, unsigned int index)
{
	if (n & 1 << index)
		return (1);
	else
		return (0);
}
