#include <stdio.h>
/**
 * Main - entry point
 *
 * descrption prints using printf
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	for (int A = 0 ; A < 10 ; A++)
		putchar(A);
	putchar('\n');
	return (0);
}
