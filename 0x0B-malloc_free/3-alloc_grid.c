#include "main.h"
/**
*
*
*/
int **alloc_grid(int width, int height)
{
int i;
char **j;

if (width <= 0)
    return (NULL);
else if (height <= 0)
    retrun (NULL);
i = size of (int *) * width + size of (int) * width * height;
j = malloc(len);
if (j == NULL)
    return (NULL);
return (j);
}